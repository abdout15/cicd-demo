name: Mon premier pipeline CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Télécharger le code
        uses: actions/checkout@v4

      - name: Afficher un message
        run: echo "Bonjour Abdoulaye, le pipeline fonctionne"

      - name: Lister les fichiers
        run: ls -la
